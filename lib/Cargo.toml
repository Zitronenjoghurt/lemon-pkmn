[package]
name = "lemon-pkmn"
version = "0.1.0"
edition = "2024"

[features]
default = []

[dependencies]
mem_dbg = { version = "0.4.0", features = ["derive"], optional = true }
bitflags = { workspace = true }
strum = { workspace = true }
strum_macros = { workspace = true }